<UserControl x:Class="RetailX.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Button Content="Add Product"
                    DockPanel.Dock="Right"
                    Width="140"
                    MinHeight="44"
                    Style="{StaticResource PrimaryButton}" />
            <StackPanel>
                <TextBlock Text="Products"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="{StaticResource InkBrush}" />
                <TextBlock Text="Search stock items by barcode, SKU, brand or product name."
                           Margin="0,3,0,0"
                           FontSize="14"
                           Foreground="{StaticResource MutedBrush}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Padding="16"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="12"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="130" />
                </Grid.ColumnDefinitions>

                <TextBox MinHeight="46"
                         FontSize="17"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1"
                        Content="Search"
                        Margin="12,0,0,0"
                        Command="{Binding SearchCommand}" />
                <Button Grid.Column="2"
                        Content="Filter"
                        Margin="12,0,0,0" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Products}"
                  RowHeight="50"
                  CanUserSortColumns="True"
                  AlternatingRowBackground="#F7FBFC">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Height" Value="44" />
                    <Setter Property="Padding" Value="12,0" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                    <Setter Property="Background" Value="#F3F8FA" />
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="BARCODE" Binding="{Binding Barcode}" Width="145" IsReadOnly="True" />
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="125" IsReadOnly="True" />
                <DataGridTextColumn Header="PRODUCT NAME" Binding="{Binding Name}" Width="*" MinWidth="240" IsReadOnly="True" />
                <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category.Name}" Width="135" IsReadOnly="True" />
                <DataGridTextColumn Header="BRAND" Binding="{Binding Brand}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="PRICE" Binding="{Binding SellingPrice, StringFormat=LKR {0:N2}}" Width="115" IsReadOnly="True" />
                <DataGridTextColumn Header="STOCK" Binding="{Binding StockQuantity, StringFormat=N0}" Width="85" IsReadOnly="True" />
                <DataGridTextColumn Header="REORDER" Binding="{Binding ReorderLevel, StringFormat=N0}" Width="90" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="ACTIVE" Binding="{Binding IsActive}" Width="80" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3"
                Padding="14"
                Margin="0,14,0,0"
                Background="{StaticResource SurfaceAltBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="10">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource MutedBrush}"
                       FontWeight="SemiBold" />
        </Border>
    </Grid>
</UserControl>
