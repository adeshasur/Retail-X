<UserControl x:Class="NexaTillPOS.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="16"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="140" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="18" />
                <Button Grid.Column="1"
                        Content="Search"
                        Margin="10,0,0,0"
                        Command="{Binding SearchCommand}" />
                <Button Grid.Column="2"
                        Content="Add Product"
                        Margin="10,0,0,0"
                        Style="{StaticResource PrimaryButton}" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Products}"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="135" IsReadOnly="True" />
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120" IsReadOnly="True" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="120" IsReadOnly="True" />
                <DataGridTextColumn Header="Selling" Binding="{Binding SellingPrice, StringFormat=N2}" Width="90" IsReadOnly="True" />
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Reorder" Binding="{Binding ReorderLevel}" Width="80" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Margin="0,12,0,0"
                   Foreground="{StaticResource MutedBrush}" />
    </Grid>
</UserControl>
